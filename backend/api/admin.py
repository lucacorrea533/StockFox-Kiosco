# admin.py registraría los modelos en el panel de administración de Django.
# No se usa en StockFox: la gestión de productos, usuarios, etc. se hace
# desde la propia aplicación web (rol Encargada), no desde este panel.

from django.contrib import admin

# Register your models here.